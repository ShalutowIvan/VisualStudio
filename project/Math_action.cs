/*
using System;
namespace Project
{
    class Math_action
    {
        static void Main()
        {
            //float userpoint;
            //userpoint = Convert.ToDouble(Console.ReadLine());
            //тип данных Double занимает много памяти и можно конверитировать с помощью Parse
            // Console.Write("Введите дробное число с точкой: ");
            //при вводе дробных чисел цифр с клавиатуры нужно вводить их ставя запятую, а не точку
            //можно сразу создать переменную
            //float userpoint = float.Parse(Console.ReadLine());
            //Console.WriteLine(userpoint);
            // float res;
            //res = userpoint + 10f;
            //res = userpoint - 11f;
            //res = userpoint * 4f;
            //res = userpoint / 2f;
            //res = userpoint % 10f;
            // int a = 5, b = 6;
            //int res = a + b;
            //сокращенная запись второго действия с переменной, при этом значение у нее изменится на новое. Также можно любую мат операцию выполнить сокращенно
            //res += 10;
            //добавление единицы к значению переменной ++, можно также отнять --
            //res++;
            //строки можно складывать, но другие операции нет возможности провести
            //Console.WriteLine(res);

            //обращение к встроенной константе(переменная, которую нельзя изменить) PI, E
            //System.Console.WriteLine(Math.PI);
            //System.Console.WriteLine(Math.E);
            //встроенная функция модуль числа, деление по модулю
            //System.Console.WriteLine(Math.Abs(-20));
            //функция Ceiling округляет число всегда к большему            
            //Console.WriteLine(Math.Ceiling(4.311f));
            //функция Floor округляет число всегда к меньшему            
            //Console.WriteLine(Math.Floor(4.311f));
            //функция Floor округляет число по математическим правилам
            //Console.WriteLine(Math.Round(4.311f));
            //есть и другие функции из математики cos, sin и тд
            //есть и другие функции из математики max, min - выводят максимальное или минималное значение
            //Console.WriteLine(Math.min(4, 5));
            //Console.WriteLine(Math.Max(4, 5));
            //функция степени pow, в скобках пишем число, потом возводим его в степеень
            // Console.WriteLine(Math.Pow(4, 5));
            //другие функции можно нагуглить
            //пример вычисления площади круга
            System.Console.WriteLine("Введите радиус круга: ");
            double radius = Convert.ToDouble(Console.ReadLine());
            double ploshad = Math.PI * Math.Pow(radius, 2);
            //в кавычка вывод переменных в фигурных скобках, 0 и 1 выводятся переменные по очереди
            //дробные числа тоже можно вводить
            System.Console.WriteLine("Площадь круга с радиусом {0} равна {1}", radius, ploshad);

        }

    }

}
*/